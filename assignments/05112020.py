# Create a module with the name str_functions with has_upper(...), has_digit(...), extract_upper(...)


import assignments.str_functions as sf

print(sf.has_upper("Peter"))
print(sf.has_upper("parker"))
print(sf.has_digit("Python 3.9"))
print(sf.has_digit("Peter"))
print(sf.extract_upper_chars(
    "Python 3.9 is the latest language. It is widely used in Data Science and Machine Learning"))
